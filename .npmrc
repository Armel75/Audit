# Configuration npm partagée (utilisée aussi dans les builds Docker)
# Miroir + désactivation SSL strict : réseau d'entreprise avec proxy HTTPS
strict-ssl=false
registry=https://registry.npmmirror.com/
