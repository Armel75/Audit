# =============================================================================
# Service du frontend web
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: audit
  labels:
    app: audit
    tier: web
spec:
  selector:
    app: audit
    tier: web
  ports:
    - name: http
      port: 80
      targetPort: 80
